import { render } from "@testing-library/react-native";
import SignIn from "../../src/pages/SingIn";

it("Test exemplo", () => {
  expect(1).toBe(1);
});

it("test render App sign component", () => {
  render(<SignIn />);
});
